# Docs

Guides for running and inspecting this project’s local data stores.

| Guide | What it covers |
| --- | --- |
| [MongoDB](./mongodb.md) | Why `mongo-data` looks like `.wt` files, `mongosh` CRUD, Compass locally, and Compass over SSH to a VPS |
